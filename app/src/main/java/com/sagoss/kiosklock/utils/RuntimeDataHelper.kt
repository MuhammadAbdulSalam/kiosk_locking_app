package com.sagoss.kiosklock.utils

import com.sagoss.kiosklock.custompager.CurrentViewPager

object RuntimeDataHelper {

    lateinit var currentViewPager: CurrentViewPager
    const val TYPE_PASSWORD = "text"
    const val TYPE_PATTERN = "pattern"
    const val TYPE_TOUCH = "touch"

}